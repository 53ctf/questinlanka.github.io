# section information
section:
  name: Featured Tours
  id: recent-posts
  enable: false
  weight: 2
  showOnNavbar: true
  # Can optionally hide the title in sections
  hideTitle: false

# no additional configuration is required
